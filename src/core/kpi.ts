export type KPIStatus='available'|'unavailable'|'manual';
export interface KPIEvidence{source:string;period:string;value:number;notes?:string}
export interface KPIInput{id:string;name:string;current:number|null;previous:number|null;unit:'number'|'currency'|'percent';evidence:KPIEvidence[];status:KPIStatus;target?:number|null}
export interface KPIResult{id:string;name:string;current:number|null;previous:number|null;change:number|null;changePct:number|null;targetGap:number|null;status:KPIStatus;formula:string;evidence:KPIEvidence[];conclusion:string}
function pct(c:number,p:number){return p===0?null:Number(((c-p)/Math.abs(p)*100).toFixed(2))}
export function calculateKPI(input:KPIInput):KPIResult{if(input.current!=null&&!Number.isFinite(input.current))throw new Error('INVALID_KPI_CURRENT: current must be finite');if(input.previous!=null&&!Number.isFinite(input.previous))throw new Error('INVALID_KPI_PREVIOUS: previous must be finite');if(input.target!=null&&!Number.isFinite(input.target))throw new Error('INVALID_KPI_TARGET: target must be finite');if(input.status==='unavailable'||input.current==null)return{...input,change:null,changePct:null,targetGap:null,formula:'Unavailable',conclusion:'Data belum cukup untuk menghitung KPI.',evidence:[...input.evidence]};const change=input.previous==null?null:Number((input.current-input.previous).toFixed(4));const changePct=change==null?null:pct(input.current,input.previous!);const targetGap=input.target==null?null:Number((input.current-input.target).toFixed(4));const conclusion=change==null?'KPI tersedia, tetapi periode pembanding tidak tersedia.':changePct==null?'Perubahan persentase tidak dapat dihitung karena periode sebelumnya bernilai nol.':`KPI berubah ${changePct>0?'+':''}${changePct}% dibanding periode sebelumnya.`;return{...input,change,changePct,targetGap,formula:change==null?'Current only':'(current - previous) / |previous| × 100%',conclusion,evidence:[...input.evidence]}}
export function calculateKPIs(inputs:KPIInput[]){return inputs.map(calculateKPI)}
