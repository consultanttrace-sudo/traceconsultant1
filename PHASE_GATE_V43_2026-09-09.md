# TRACE Phase Gate v43 — Root Cause Verification

## Completed in this checkpoint
- Root-cause audit performed against v42 source, tests, package/environment and design implementation.
- Intelligence scoring corrected so missing dimensions are not treated as zero.
- Marketing invalid cost can no longer produce ROI/ROAS from a partial denominator.
- Workbook multi-sheet merge now requires matching business/outlet/period identity.
- Release verification no longer performs an unbounded implicit `npm install`; environment blockers fail fast and are labeled BLOCKED.
- React shell premium baseline strengthened with semantic brand/command/navigation states and focus accessibility.
- Core regression suite re-run after fixes.

## Concrete blockers
1. Root React dependencies are absent (`node_modules` missing) and there is no root lockfile. This prevents a reproducible Vite/React build in the current environment.
2. Browser harness dependency `jsdom` is unavailable after offline dependency restoration; npm reported an uncached transitive artifact and the runtime is Node 22.16.0 while the locked harness requires a newer Node 22 minor.
3. Live Supabase RLS and operational-job durability are not executable without an actual project/Auth identities.
4. Live Netlify deploy/health/rollback cannot be proven without production/staging configuration.
5. React premium parity is not complete across all TRACE modules. Legacy production UI remains until each module reaches functional parity + premium design + regression proof.

## Release rule
This checkpoint is **NOT production-ready**. A green deterministic core suite does not override environment-dependent blockers.

## Required next verification
- Generate and commit a root `package-lock.json` in a network-enabled controlled environment.
- Pin and test dependency versions; use `npm ci` rather than floating `latest` for release builds.
- Run React typecheck + Vite build on Node version satisfying all declared/tested engines.
- Execute browser harness.
- Execute Supabase RLS matrix with real Auth identities.
- Execute Acquisition crash/reload/resume against staging Supabase.
- Execute Netlify deployment on staging, poll result, run smoke tests, and verify rollback path.
- Continue React module-by-module premium parity before legacy retirement.
